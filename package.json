{
  "name": "mcp-census",
  "version": "1.0.0",
  "description": "MCP server wrapping the US Census Bureau API for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-census": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["mcp", "census", "api", "demographics", "population"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.18"
  }
}
